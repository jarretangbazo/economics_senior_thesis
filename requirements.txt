# =============================================================================
# requirements.txt — Python dependencies for Education Under Fire
#
# Install with:  pip install -r requirements.txt
# Freeze with:   pip freeze > requirements.txt
#
# Core versions pinned for reproducibility. Upgrade intentionally, not silently.
# =============================================================================

# --- Data manipulation and numerics ---
pandas>=2.0
numpy>=1.24
pyreadstat>=1.2		# read .dta Stata files

# --- Geospatial work ---
geopandas>=0.14.0       # Spatial joins, shapefiles, geometry operations
shapely>=2.0.0          # Geometry primitives used by geopandas
pyproj>=3.5.0           # Coordinate reference system transformations
fiona>=1.9.0            # Reading/writing shapefiles (used by geopandas)

# --- Raster and travel-time ---
rasterio>=1.3.0         # Read/write GeoTIFF rasters (friction surfaces)
scikit-image>=0.22.0    # skimage.graph.MCP_Geometric for least-cost paths

# --- Visualisation ---
matplotlib>=3.7.0       # Base plotting library
seaborn>=0.12.0         # Statistical plot helpers built on matplotlib
contextily>=1.5.0       # Adds basemap tiles to geopandas plots

# --- Jupyter notebook (EDA only) ---
jupyter>=1.0
nbstripout>=0.6

# --- Configuration and environment ---
python-dotenv>=1.0.0    # Load variables from .env file (API keys, paths)
tqdm>=4.56.0            # Progress bars for long-running loops
thefuzz>=0.20		# fuzzy string matching for LGA name reconciliation
